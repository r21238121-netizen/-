# Futures Scout - Документация по функциональности

## Обзор проекта

Futures Scout - это локальный десктопный терминал для Windows, позволяющий подключаться к бирже BingX через API и использовать встроенный ИИ-модуль для анализа монет и получения точек входа в реальном времени.

## Новая функциональность: Обучение ИИ из JSON датасетов

### 1. Генерация датасета

Скрипт `generate_dataset.py` создает обучающие датасеты с рыночными признаками и историей сигналов:

- Генерирует 10,000 обучающих примеров по умолчанию
- Включает рыночные признаки: цены, индикаторы, объемы, стакан, фондирование
- Содержит информацию о сигналах: направление, точки входа/выхода, успешность
- Создает как обучающий, так и тестовый датасеты

### 2. Обучение ИИ модели

Скрипт `train_ai_model.py` позволяет:

- Загружать JSON датасеты для обучения
- Обучать XGBoost модель на основе признаков
- Оценивать производительность модели
- Сравнивать с существующей моделью
- Сохранять обученную модель

### 3. Интеграция в exe файл

В исполняемом файле (.exe) доступна функция:

- Загрузка JSON датасетов через GUI
- Обучение ИИ модели без перезапуска приложения
- Прогресс-отслеживание процесса обучения
- Безопасное выполнение в отдельном потоке

### 4. Использование в приложении

В интерфейсе добавлена кнопка "📥 Загрузить датасет для ИИ" которая:

- Открывает диалог выбора JSON файла
- Показывает прогресс обучения
- Обновляет модель без перезапуска приложения
- Отображает результаты обучения

## API BingX

### WebSocket Market Data
- Публичный: `wss://open-api-swap.bingx.com/swap-market`

### REST API (публичный)
- HTTP GET: `https://open-api.bingx.com/openApi/future/v1/...`

### REST API (приватный)
- HTTP POST/GET: `https://open-api.bingx.com/openApi/future/v1/...` + подпись

## Структура проекта

```
/workspace/
├── generate_dataset.py          # Генерация обучающего датасета
├── train_ai_model.py           # Обучение ИИ модели из датасета
├── integrated_ai_training.py   # Интеграция в приложение
├── test_ai_training.py         # Тестирование функциональности
├── datasets/                   # Сгенерированные датасеты
├── src/
│   ├── main.py                 # Основной файл запуска
│   ├── api/
│   │   └── bingx_api.py       # Взаимодействие с API BingX
│   ├── gui/
│   │   ├── auth_window.py     # Окно аутентификации
│   │   ├── main_window.py     # Основное окно
│   │   └── training_dialog.py # Диалог загрузки датасета
│   ├── models/
│   │   └── ai_agent.py        # ИИ-агент с обучением из датасета
│   ├── ui/                     # Виджеты интерфейса
│   └── utils/                  # Утилиты
└── build.py                    # Скрипт упаковки в .exe
```

## ИИ-модуль самообучения

ИИ-модуль автоматически улучшает точность прогнозов на основе реальных исходов сделок:

- Генерация сигналов с указанием вероятности успеха
- Отслеживание результатов сделок
- Периодическое переобучение модели на основе истории
- Возможность загрузки внешних датасетов для обучения
- Сохранение версионности модели

## Безопасность

- При первом запуске показывается предупреждение о рисках
- API-ключи шифруются и хранятся локально
- Никакие данные не передаются в сеть, кроме запросов к BingX
- Поля ввода API-ключей маскируются

## Упаковка в .exe

Для упаковки приложения в исполняемый файл Windows:

```bash
python build.py
```

Результат будет находиться в папке `dist/FuturesScout`.

## Требования

- Python 3.10+
- Windows 10/11 (64-bit)